[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# startCommandをコメントアウトしてDockerfileのCMDを使用
# DockerfileのCMDがフロントエンドとバックエンドの両方を起動します
# startCommand = "python backend/app.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
